import { View } from 'react-native'
import styled from 'styled-components/native'

import { AppBar } from '../styleguide'

export const PageWrapper = View

export const TopAppBar = styled(AppBar)`
	top: 0;
	bottom: auto;
`;
